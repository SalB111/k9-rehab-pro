# TAB INDEX IMPLEMENTATION GUIDE

## STEP 1: PATIENT INFO - Tab Order

### Client Information
1. Client Name (tabIndex={1})
2. Address (tabIndex={2})
3. Zip Code (tabIndex={3})
4. City (tabIndex={4})
5. State (tabIndex={5})
6. Client Email (tabIndex={6})
7. Client Phone (tabIndex={7})
8. Hospital Dropdown (tabIndex={8})
9. Manual Vet Entry (tabIndex={9})

### Patient Information
10. Patient Name (tabIndex={10})
11. Breed Select (tabIndex={11})
12. Age (tabIndex={12})
13. Weight LBS (tabIndex={13})
14. Weight KG (tabIndex={14})
15. Sex Buttons (tabIndex={15-18})

## STEP 2: CLINICAL ASSESSMENT - Tab Order

20. Pain Level Slider (tabIndex={20})
21. Lameness Score Select (tabIndex={21})
22. BCS Score Slider (tabIndex={22})
23. Muscle Mass Select (tabIndex={23})
24. Muscle Tone Select (tabIndex={24})
25. Joint ROM Select (tabIndex={25})
26. Activity Level Slider (tabIndex={26})
27. Ambulation Status Select (tabIndex={27})
28. Proprioception Assessment (tabIndex={28})

## STEP 3: DIAGNOSIS - Tab Order

30. Condition Checkboxes (tabIndex={30+})
   - Each checkbox gets sequential tabIndex

## STEP 4: TREATMENT GOALS - Tab Order

50. Goal Checkboxes (tabIndex={50+})
   - Each checkbox gets sequential tabIndex

## NAVIGATION BUTTONS

100. Back Button (tabIndex={100})
101. Next/Submit Button (tabIndex={101})
